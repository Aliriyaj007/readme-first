# readme-first
A CLI that checks why users can’t run your repo in 2 minutes. Scores your README, finds missing steps, and generates a copy-paste Quick Start. Fix first-run friction before users leave.
